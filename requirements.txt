# ---- The API ----------------------------------------------------------------
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.6,<3
prometheus-client>=0.20

# ---- The analyst ------------------------------------------------------------
# The one optional runtime dependency: with no GOOGLE_API_KEY the app never
# constructs a client and serves from the offline mock instead.
google-genai>=0.3

# ---- Retrieval --------------------------------------------------------------
# Moss: semantic search over the candidate pool. Optional at runtime -- with
# no MOSS_PROJECT_* credentials the app never constructs a client and uses its
# local lexical index instead.
#
# Its runtime (inferedge-moss-core) is a native manylinux_2_35 wheel, so it
# needs glibc >= 2.35 and will not install on Alpine/musl. Dockerfile.api is
# on python:3.11-slim (Debian bookworm, glibc 2.36), which is why.
moss>=1.12

# ---- Persistence ------------------------------------------------------------
sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9       # Postgres driver for production DATABASE_URL;
                           # SQLite needs nothing extra

# ---- HTTP client ------------------------------------------------------------
# Required by fastapi.testclient. httpx2/anyio are pinned because newer
# starlette resolves its TestClient transport through them.
httpx>=0.27
httpx2>=2.13,<3
anyio>=4.6

# ---- Test + lint ------------------------------------------------------------
pytest>=8.0
ruff>=0.4
