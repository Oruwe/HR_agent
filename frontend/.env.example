# Base URL of the API Gateway (app/api/app.py). Leave empty to call the same
# origin the frontend is served from (e.g. behind a reverse proxy that routes
# /api to the backend). For local dev against `uvicorn app.api.app:app`, use:
VITE_API_BASE_URL=http://localhost:8000
